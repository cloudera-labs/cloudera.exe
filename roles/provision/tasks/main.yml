---

- name: Deprecation warning
  run_once: true
  cloudera.exe.deprecation:
    msg: "Role, {{ ansible_role_name }}, is no longer supported."
    version: 4.0.0

- name: Execute provisioning state
  ansible.builtin.include_tasks: "{{ provision_state }}.yml"
